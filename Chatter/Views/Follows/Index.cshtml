@model IEnumerable<Chatter.Models.Follow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Photo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User1.Photo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Photo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User1.Photo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FollowID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FollowID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FollowID })
        </td>
    </tr>
}

</table>
